---
title: "Mark as Read"
api: "PATCH /v1/notifications/mark-read"
description: "Mark notifications as read"
---

## Overview

Mark one or more notifications as read. This can be done for a single notification or multiple notifications at once.

## Request Body

<ParamField body="notification_ids" type="array" required>
  Array of notification IDs to mark as read
</ParamField>

<ParamField body="mark_all" type="boolean">
  Mark all notifications as read (ignores notification_ids)
</ParamField>

<ParamField body="type" type="string">
  Mark all notifications of a specific type as read
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Indicates if the operation was successful
</ResponseField>

<ResponseField name="message" type="string">
  Confirmation message
</ResponseField>

<ResponseField name="updated_count" type="number">
  Number of notifications marked as read
</ResponseField>

<ResponseField name="updated_at" type="string">
  Update timestamp (ISO 8601)
</ResponseField>

## Example Request

<CodeGroup>

```bash cURL
# Mark specific notifications as read
curl -X PATCH "https://api.railsfromthecrypt.com/v1/notifications/mark-read" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "notification_ids": ["notif_123e4567-e89b-12d3-a456-426614174000", "notif_123e4567-e89b-12d3-a456-426614174001"]
  }'

# Mark all notifications as read
curl -X PATCH "https://api.railsfromthecrypt.com/v1/notifications/mark-read" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "mark_all": true
  }'
```

```javascript JavaScript
// Mark specific notifications as read
const response = await fetch('https://api.railsfromthecrypt.com/v1/notifications/mark-read', {
  method: 'PATCH',
  headers: {
    'Authorization': 'Bearer YOUR_ACCESS_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    notification_ids: ['notif_123e4567-e89b-12d3-a456-426614174000', 'notif_123e4567-e89b-12d3-a456-426614174001']
  })
});

const result = await response.json();

// Mark all notifications as read
const markAllResponse = await fetch('https://api.railsfromthecrypt.com/v1/notifications/mark-read', {
  method: 'PATCH',
  headers: {
    'Authorization': 'Bearer YOUR_ACCESS_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    mark_all: true
  })
});

const markAllResult = await markAllResponse.json();
```

</CodeGroup>

## Example Response

<ResponseExample>

```json Success Response
{
  "success": true,
  "message": "Notifications marked as read successfully",
  "updated_count": 2,
  "updated_at": "2024-01-01T12:30:00Z"
}
```

</ResponseExample>

## Usage Examples

### Mark Single Notification as Read

<CodeGroup>

```javascript JavaScript
async function markNotificationAsRead(notificationId) {
  try {
    const response = await fetch('https://api.railsfromthecrypt.com/v1/notifications/mark-read', {
      method: 'PATCH',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        notification_ids: [notificationId]
      })
    });

    const result = await response.json();
    
    if (result.success) {
      // Update UI to reflect read status
      updateNotificationStatus(notificationId, 'read');
      updateNotificationCounts();
    }
    
    return result;
  } catch (error) {
    console.error('Error marking notification as read:', error);
    throw error;
  }
}
```

</CodeGroup>

### Mark All Notifications as Read

<CodeGroup>

```javascript JavaScript
async function markAllNotificationsAsRead() {
  try {
    const response = await fetch('https://api.railsfromthecrypt.com/v1/notifications/mark-read', {
      method: 'PATCH',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        mark_all: true
      })
    });

    const result = await response.json();
    
    if (result.success) {
      // Update UI to reflect all notifications as read
      updateAllNotificationsStatus('read');
      updateNotificationCounts();
    }
    
    return result;
  } catch (error) {
    console.error('Error marking all notifications as read:', error);
    throw error;
  }
}
```

</CodeGroup>

## Related Endpoints

<CardGroup cols={2}>
  <Card
    title="List Notifications"
    icon="list"
    href="/api/notifications/list"
  >
    Get list of all notifications
  </Card>
  <Card
    title="Notification Count"
    icon="bell"
    href="/api/notifications/count"
  >
    Get notification counts by status
  </Card>
</CardGroup>

