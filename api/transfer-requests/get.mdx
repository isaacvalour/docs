---
title: "Get Transfer Request"
api: "GET /v1/transfer-requests/{id}"
description: "Get specific transfer request details by ID"
---

## Overview

Retrieve detailed information about a specific transfer request using its unique ID.

## Path Parameters

<ParamField path="id" type="string" required>
  Unique identifier for the transfer request
</ParamField>

## Response

<ResponseField name="id" type="string">
  Transfer request ID
</ResponseField>

<ResponseField name="reference" type="string">
  Transfer reference
</ResponseField>

<ResponseField name="status" type="string">
  Transfer status
</ResponseField>

<ResponseField name="amount" type="string">
  Transfer amount
</ResponseField>

<ResponseField name="currency" type="string">
  Source currency
</ResponseField>

<ResponseField name="rate" type="string">
  Exchange rate used
</ResponseField>

<ResponseField name="received_amount" type="string">
  Amount recipient will receive
</ResponseField>

<ResponseField name="fee" type="string">
  Transfer fee
</ResponseField>

<ResponseField name="recipient" type="object">
  Recipient information
</ResponseField>

<ResponseField name="payment_instructions" type="object">
  Payment instructions
</ResponseField>

<ResponseField name="created_at" type="string">
  Creation timestamp (ISO 8601)
</ResponseField>

<ResponseField name="updated_at" type="string">
  Last update timestamp (ISO 8601)
</ResponseField>

<ResponseField name="expires_at" type="string">
  Payment expiration timestamp (ISO 8601)
</ResponseField>

## Example Request

<CodeGroup>

```bash cURL
curl -X GET "https://api.railsfromthecrypt.com/v1/transfer-requests/tr_123e4567-e89b-12d3-a456-426614174000" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://api.railsfromthecrypt.com/v1/transfer-requests/tr_123e4567-e89b-12d3-a456-426614174000', {
  headers: {
    'Authorization': 'Bearer YOUR_ACCESS_TOKEN'
  }
});

const transferRequest = await response.json();
```

</CodeGroup>

## Example Response

<ResponseExample>

```json Success Response
{
  "id": "tr_123e4567-e89b-12d3-a456-426614174000",
  "reference": "TXN-1234567890",
  "status": "pending",
  "amount": "1000.00",
  "currency": "NGN",
  "rate": "0.0008",
  "received_amount": "0.80",
  "fee": "10.00",
  "recipient": {
    "id": "rec_123e4567-e89b-12d3-a456-426614174000",
    "name": "John Doe",
    "email": "john.doe@example.com"
  },
  "payment_instructions": {
    "amount": "1000.00",
    "currency": "NGN",
    "reference": "TXN-1234567890",
    "account_details": {
      "bank_name": "First Bank of Nigeria",
      "account_number": "1234567890",
      "account_name": "HashRails Limited",
      "routing_code": "011"
    }
  },
  "created_at": "2024-01-01T12:00:00Z",
  "updated_at": "2024-01-01T12:00:00Z",
  "expires_at": "2024-01-01T12:05:00Z"
}
```

</ResponseExample>

## Related Endpoints

<CardGroup cols={2}>
  <Card
    title="List Transfer Requests"
    icon="list"
    href="/api/transfer-requests/list"
  >
    Get list of all transfer requests
  </Card>
  <Card
    title="Confirm Payment"
    icon="check"
    href="/api/transfer-requests/confirm-payment"
  >
    Confirm payment for transfer request
  </Card>
</CardGroup>
