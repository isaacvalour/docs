---
title: "Alatpay Webhook"
api: "POST /v1/providers/alatpay"
description: "Handle webhook events from Alatpay payment provider"
---

## Overview

Receive and process webhook events from Alatpay payment provider. This endpoint handles payment notifications, transaction updates, and other events from Alatpay.

<Note>
This endpoint is designed to receive webhooks from Alatpay and does not require authentication. It's used internally by the system to process provider events.
</Note>

## Request Headers

<ParamField header="X-Alatpay-Signature" type="string" required>
  Webhook signature for verification
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`
</ParamField>

## Request Body

<ParamField body="event_type" type="string" required>
  Type of event from Alatpay
</ParamField>

<ParamField body="data" type="object" required>
  Event data payload
</ParamField>

<ParamField body="timestamp" type="string" required>
  Event timestamp from Alatpay
</ParamField>

<ParamField body="signature" type="string" required>
  Event signature for verification
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Indicates if the webhook was processed successfully
</ResponseField>

<ResponseField name="message" type="string">
  Processing result message
</ResponseField>

<ResponseField name="processed_at" type="string">
  Processing timestamp (ISO 8601)
</ResponseField>

## Example Request

<CodeGroup>

```bash cURL
curl -X POST "https://api.railsfromthecrypt.com/v1/providers/alatpay" \
  -H "X-Alatpay-Signature: sha256=abc123..." \
  -H "Content-Type: application/json" \
  -d '{
    "event_type": "payment.completed",
    "data": {
      "transaction_id": "alatpay_txn_123",
      "amount": "2000.00",
      "currency": "NGN",
      "status": "completed"
    },
    "timestamp": "2024-01-01T12:00:00Z",
    "signature": "sha256=def456..."
  }'
```

</CodeGroup>

## Example Response

<ResponseExample>

```json Success Response
{
  "success": true,
  "message": "Webhook processed successfully",
  "processed_at": "2024-01-01T12:00:01Z"
}
```

</ResponseExample>

## Event Types

<CardGroup cols={2}>
  <Card title="Payment Events" icon="credit-card">
    - `payment.completed`
    - `payment.failed`
    - `payment.pending`
    - `payment.cancelled`
  </Card>
  <Card title="Transaction Events" icon="exchange-alt">
    - `transaction.created`
    - `transaction.updated`
    - `transaction.completed`
    - `transaction.failed`
  </Card>
</CardGroup>

## Related Endpoints

<CardGroup cols={2}>
  <Card
    title="Juicyway Webhook"
    icon="juice"
    href="/api/providers/juicyway"
  >
    Handle Juicyway provider webhooks
  </Card>
  <Card
    title="Sumsub Webhook"
    icon="sumsub"
    href="/api/providers/sumsub"
  >
    Handle Sumsub provider webhooks
  </Card>
</CardGroup>

